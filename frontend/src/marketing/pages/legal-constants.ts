/*
 * Facts the three legal documents share. They are here rather than repeated per
 * page so the effective date and the contact addresses cannot drift apart —
 * three pages disagreeing about which version is current is exactly the kind of
 * inconsistency a published policy cannot have.
 *
 * `hello@` is the address already published on the contact page. The privacy
 * and legal aliases are separate so rights requests and notices are routable
 * without being lost in general enquiries.
 */

/** The date this version of all three documents took effect. */
export const EFFECTIVE_DATE = '29 July 2026';

export const CONTACT_EMAIL = 'hello@martgloballlc.com';
export const PRIVACY_EMAIL = 'privacy@martgloballlc.com';
export const LEGAL_EMAIL = 'legal@martgloballlc.com';
